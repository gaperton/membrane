node_modules/
dist/
coverage/
data/
.env
*.log

# Swift package build artifacts. Package.resolved is committed so the
# generated client is reproducible from pinned dependency versions.
swift/.build/
swift/.swiftpm/
